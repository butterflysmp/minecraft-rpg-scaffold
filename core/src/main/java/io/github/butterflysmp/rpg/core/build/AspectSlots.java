package io.github.butterflysmp.rpg.core.build;

/**
 * A cell's aspect slots: two. {@code CellLoadout.ASPECTS} stores the same two; a paper test pins the two
 * equal, as it does for fragments.
 */
public final class AspectSlots {

    private AspectSlots() {}

    public static final int COUNT = 2;
}
